<!-- UserControl→PanelBaseへ変更(2025.7.28yori) -->
<!-- 全体のFontSize="20"へ変更(2025.9.28yori) -->
<local:PanelBase
			 x:Class="VecApp.InitializePanel"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:VecApp"
             xmlns:res="clr-namespace:VecApp.Properties"
             mc:Ignorable="d"
             d:DesignHeight="480" d:DesignWidth="800">
    <Grid>
        <Border Background="#2C3E50" CornerRadius="10" Padding="20">
            <StackPanel VerticalAlignment="Top" HorizontalAlignment="Stretch">

                <TextBlock Text="{x:Static res:Resources.String1}"
                    FontSize="20"
                    FontWeight="Bold"
                    Foreground="White"
                    Margin="0,0,0,10"/>

                <Border BorderBrush="Gray" BorderThickness="1" CornerRadius="6" Padding="10" Height="396">
                    <StackPanel>
                        <!-- 仮の画像スペース -->
                        <Grid Height="252" Background="#DDD">
                            <Image Source="{Binding ImageSource}"
                                 Width="800" Height="252"
                                 Stretch="Uniform"
                                 Canvas.Left="0"
                                 Canvas.Top="0"
                                 Canvas.ZIndex="5"/>

                            <ItemsControl ItemsSource="{Binding Marks}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <Canvas />
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                                <ItemsControl.ItemContainerStyle>
                                    <Style TargetType="ContentPresenter">
                                        <Setter Property="Canvas.Left" Value="{Binding X}" />
                                        <Setter Property="Canvas.Top" Value="{Binding Y}" />
                                    </Style>
                                </ItemsControl.ItemContainerStyle>
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <TextBlock Text="{Binding Text}"
                                           Visibility="{Binding Visibility}"
                                           FontSize="40"
                                           FontWeight="Bold"
                                           Foreground="Black"
                                           Canvas.ZIndex="10"/>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                        </Grid>

                        <ItemsControl ItemsSource="{Binding Labels}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center"/>
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding Text}"
                                         Visibility="{Binding Visibility}"
                                         FontSize="50"
                                         FontWeight="Normal"
                                         Foreground="White"
                                         Margin="30,0"/>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>

                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <StackPanel Orientation="Horizontal" Grid.Column="0" VerticalAlignment="Center">
                                <TextBlock Text="{x:Static res:Resources.String2}" VerticalAlignment="Center" FontSize="20" Margin="0,0,10,0" Foreground="White"/>

                                <!-- スライドバー -->
                                <Slider
                                    Width="80"
                                    Minimum="0" Maximum="1"
                                    TickFrequency="1"
                                    IsSnapToTickEnabled="True"
                                    ValueChanged="SlideSwitch_ValueChanged"
                                    Value="{Binding _SlideSwitch}" VerticalAlignment="Center"/>

                                <TextBlock
                                    Text="{Binding _SlideText}"
                                    Foreground="White"
                                    FontWeight="Bold"
                                    VerticalAlignment="Center"
                                    Margin="5,0,0,0"/>
                            </StackPanel>

                            <Button Content="{x:Static res:Resources.String56}" Margin="600,0,7,0" Width="130" Height="50"
                              Click="Click_CancelBtn" Style="{StaticResource PanelCancelButtonStyle}" VerticalAlignment="Top" FontSize="20"/>
                        </Grid>
                    </StackPanel>
                </Border>

            </StackPanel>
        </Border>
    </Grid>
</local:PanelBase>
