<!-- 全体のFontSize="20"へ変更(2025.9.28yori) -->
<local:PanelBase
        x:Class="VecApp.SensorConnectionPanel"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:VecApp"
        xmlns:res="clr-namespace:VecApp.Properties"
        mc:Ignorable="d"
        d:DesignHeight="480" d:DesignWidth="800">
    <Grid>
        <Border Background="#2C3E50" CornerRadius="10" Padding="20">
            <StackPanel VerticalAlignment="Top" HorizontalAlignment="Stretch">

                <!-- 用語(センサー→スキャナ)変更(2025.7.28yori)-->
                <TextBlock Text="{x:Static res:Resources.String3}"
                           FontSize="20"
                           FontWeight="Bold"
                           Foreground="White"
                           Margin="0,0,0,10"/>

                <Border BorderBrush="Gray" BorderThickness="1" CornerRadius="6" Padding="10">
                    <StackPanel>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="200" />
                            </Grid.ColumnDefinitions>

                            <!-- 用語(センサー→スキャナ)変更(2025.7.28yori)-->
                            <StackPanel Grid.Column="0" VerticalAlignment="Top" Margin="0,0,150,0" Height="180">
                                <TextBlock Text="{x:Static res:Resources.String4}" Foreground="White" FontSize="26" Margin="0,0,0,10" Background="Gray"/>
                                <TextBlock Text="{x:Static res:Resources.String5}" Foreground="White" FontSize="20" Margin="15,0,0,20"/>
                                <TextBlock Text="{x:Static res:Resources.String6}" Foreground="White" FontSize="20" Margin="15,0,0,20"/>
                                <TextBlock Text="{x:Static res:Resources.String7}" Foreground="White" FontSize="20" Margin="15,0,0,20"/>
                            </StackPanel>

                            <!-- 仮の画像スペース -->
                            <!-- CornerRadius="6"削除(2025.10.28yori) -->
                            <Border Background="#DDD" Margin="415,0,20,0" Grid.ColumnSpan="2">
                                <!-- Image/connectSensor_Api_V8.PNG→{Binding Init0Image}へ変更(2025.10.28yori) -->
                                <Image Source="{Binding ConnectScannerImage}"
                                       x:Name="PngImage"
                                       Stretch="Uniform"      
                                       HorizontalAlignment="Left"
                                       VerticalAlignment="Top"
                                       Height="240" Margin="0,0,0,0"/>

                            </Border>
                        </Grid>

                        <!-- 注意事項説明文変更(2025.7.28yori) -->
                        <TextBlock Text="{x:Static res:Resources.String8}"
                               Foreground="White"
                               FontSize="20"
                               Margin="415,10,0,0"/>

                        <!-- Width変更(2025.12.14yori) -->
                        <Button Content="{x:Static res:Resources.String58}"
                            HorizontalAlignment="Left"
                            Padding="16,8"
                            Margin="0,-160,0,0"
                            Click="Click_ConnectDoneBtn" Width="160" Style="{StaticResource PanelDoneButtonStyle}" Height="46" FontSize="20"/>

                        <!-- キャンセルボタン追加(2025.7.30yori) -->
                        <!-- Width変更(2025.12.14yori) -->
                        <Button Content="{x:Static res:Resources.String56}"
                            HorizontalAlignment="Left"
                            Padding="16,8"
                            Margin="170,-160,0,0"
                            Click="Click_ConnectCancelBtn" Width="160" Style="{StaticResource PanelCancelButtonStyle}" Height="46" FontSize="20" HorizontalContentAlignment="Center"/>

                        <!-- HorizontalAlignment="Right"追加(2025.12.14yori) -->
                        <!-- 未作成のため、半透明化(2025.12.14yori) -->
                        <StackPanel Orientation="Horizontal" Grid.Column="0" HorizontalAlignment="Right">
                            <TextBlock Text="{x:Static res:Resources.String2}" FontSize="20" Margin="480,20,10,0" Foreground="White" Opacity="0.25"/>

                            <!-- スライドバー -->
                            <!-- 未作成のため、無効化、半透明化(2025.12.14yori) -->
                            <Slider x:Name="SlideSwitch"
                                Width="80"
                                Minimum="0" Maximum="1"
                                TickFrequency="1"
                                IsSnapToTickEnabled="True"
                                ValueChanged="SlideSwitch_ValueChanged"
                                Margin="5,27,0,0" IsEnabled="False" Opacity="0.25"/>
                            <!-- Text="オフ"変更(2025.12.14yori) -->
                            <!-- 未作成のため、半透明化(2025.12.14yori) -->
                            <TextBlock x:Name="SlideStatusText"
                                   Text="{x:Static res:Resources.String144}"
                                   Foreground="White"
                                   FontWeight="Bold"
                                   Margin="5,20,0,0" FontSize="20" Opacity="0.25"/>
                        </StackPanel>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Border>
    </Grid>
</local:PanelBase>
