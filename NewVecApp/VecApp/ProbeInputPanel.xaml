<local:PanelBase
        x:Class="VecApp.ProbeInputPanel"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:VecApp"
        xmlns:res="clr-namespace:VecApp.Properties"
        mc:Ignorable="d"
        d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Border Background="#2C3E50" CornerRadius="10" Padding="20">
            <StackPanel VerticalAlignment="Top" HorizontalAlignment="Stretch">
                <TextBlock Text="{x:Static res:Resources.String74}"
                    FontSize="12"
                    FontWeight="Bold"
                    Foreground="White"
                    Margin="0,0,0,10"/>

                <Border BorderBrush="Gray" BorderThickness="1" CornerRadius="6" Padding="10" Height="206">
                    <StackPanel>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="*"/>
                            </Grid.ColumnDefinitions>

							<!-- ViewModel対応による追加と変更(2025.7.29yori) -->
                            <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="0,0,76,0" Height="202" Grid.ColumnSpan="3">
                                <StackPanel Orientation="Horizontal" Margin="15,0,0,5">
                                    <TextBlock Text="{x:Static res:Resources.String11}" Foreground="White" FontSize="16" VerticalAlignment="Center"/>
                                    <TextBox Width="70"
                                        FontSize="16"
                                        Margin="56,0,0,0"
                                        VerticalContentAlignment="Center"
                                        Background="Black"
                                        Foreground="White"
                                        Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="15,10,0,5">
                                    <TextBlock Text="{x:Static res:Resources.String12}" Foreground="White" FontSize="16" VerticalAlignment="Center"/>
                                    <TextBox Width="70"
                                        FontSize="16"
                                        Margin="73,0,0,0"
                                        VerticalContentAlignment="Center"
                                        Background="Black"
                                        Foreground="White"
                                        Text="{Binding Id, UpdateSourceTrigger=PropertyChanged}"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="15,10,0,5">
                                    <TextBlock Text="{x:Static res:Resources.String75}" Foreground="White" FontSize="16" VerticalAlignment="Center"/>
                                    <TextBox Width="70"
                                        FontSize="16"
                                        Margin="20,0,0,0"
                                        VerticalContentAlignment="Center"
                                        Background="Black"
                                        Foreground="White"
                                        Text="{Binding BallDiameter, UpdateSourceTrigger=PropertyChanged}"/>
                                    <TextBlock Text="mm" Foreground="White" FontSize="16" VerticalAlignment="Center" Margin="10,0,0,0"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="15,10,0,5">
                                    <TextBlock Text="{x:Static res:Resources.String76}" Foreground="White" FontSize="16" VerticalAlignment="Center"/>
                                    <ComboBox ItemsSource="{Binding BallItems}" SelectedIndex="{Binding BallIndex}" Width="152" FontSize="16" Margin="10,0,0,0"/>
                                </StackPanel>
                            </StackPanel>

                            <!-- 仮の画像スペース -->
                            <Border Background="#DDD" Margin="46,10,46,47" CornerRadius="6" Grid.Column="1">
                                <Image Source="{Binding ImageSource}"
                                   Stretch="Uniform"
                                   VerticalAlignment="Center"
                                   HorizontalAlignment="Center"/>
                            </Border>

                            <StackPanel Grid.Column="2" VerticalAlignment="Top" Orientation="Vertical" Margin="0,0,0,0">
                                <Button Content="{x:Static res:Resources.String59}"
                                    Margin="0,50,0,5"
                                    Width="200" Height="40" Click="Click_StartBtn" Style="{StaticResource PanelStartButtonStyle}"/>
                                <Button Content="{x:Static res:Resources.String56}"
                                    Margin="0,10,0,5"
                                    Width="200" Height="40" Click="Click_CancelBtn" Style="{StaticResource PanelCancelButtonStyle}"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Border>
    </Grid>
</local:PanelBase>
