<!-- 全体のFontSize="20"へ変更(2025.10.24yori) -->
<local:PanelBase
        x:Class="VecApp.ProbeInputPanel"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:VecApp"
        xmlns:res="clr-namespace:VecApp.Properties"
        mc:Ignorable="d"
        d:DesignHeight="370" d:DesignWidth="800">
    <Grid>
        <Border Background="#2C3E50" CornerRadius="10" Padding="20">
            <StackPanel VerticalAlignment="Top" HorizontalAlignment="Stretch">
                <!-- ViewModel対応による追加と変更(2025.7.29yori) -->
                <TextBlock Text="{x:Static res:Resources.String70}"
                    FontSize="20"
                    FontWeight="Bold"
                    Foreground="White"
                    Margin="0,0,0,10"/>

                <!-- Height="206"→"290"へ変更(2025.10.27yori) -->
                <Border BorderBrush="Gray" BorderThickness="1" CornerRadius="6" Padding="10" Height="290">
                    <StackPanel>
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="1.5*"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="1.1*"/>
                            </Grid.ColumnDefinitions>

							<!-- ViewModel対応による追加と変更(2025.7.29yori) -->
                            <!-- Height="206"→"300"へ変更(2025.10.27yori) -->
                            <StackPanel Grid.Column="0" VerticalAlignment="Center" Margin="0,0,76,0" Height="300" Grid.ColumnSpan="3">
                                <StackPanel Orientation="Horizontal" Margin="15,0,0,5">
                                    <TextBlock Text="{x:Static res:Resources.String11}" Foreground="White" FontSize="20" VerticalAlignment="Center"/>
                                    <!-- Width="70"→Width="200"へ変更(2025.10.24yori) -->
                                    <TextBox Width="200"
                                        FontSize="20"
                                        Margin="56,0,0,0"
                                        VerticalContentAlignment="Center"
                                        Background="Black"
                                        Foreground="White"
                                        Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="15,10,0,5">
                                    <TextBlock Text="{x:Static res:Resources.String12}" Foreground="White" FontSize="20" VerticalAlignment="Center"/>
                                    <!-- プローブIDは変更できないよう無効(2025.10.31yori) -->
                                    <TextBox Width="70"
                                        FontSize="20"
                                        Margin="73,0,0,0"
                                        VerticalContentAlignment="Center"
                                        Background="Black"
                                        Foreground="White"
                                        Text="{Binding Id, UpdateSourceTrigger=PropertyChanged}"
                                        IsEnabled="False"/>
                                </StackPanel>
                                <!-- 角度追加(2025.10.27yori) -->
                                <!-- 角度は仕様不明なため無効(2025.10.31yori) -->
                                <StackPanel Orientation="Horizontal" Margin="15,10,0,5">
                                    <TextBlock Text="{x:Static res:Resources.String18}" Foreground="White" FontSize="20" VerticalAlignment="Center" Opacity="0.25"/>
                                    <TextBox Width="70"
                                        FontSize="20"
                                        Margin="20,0,0,0"
                                        VerticalContentAlignment="Center"
                                        Background="Black"
                                        Foreground="White"
                                        Text="{Binding ProbeAngle, UpdateSourceTrigger=PropertyChanged}" IsEnabled="False"/>
                                    <TextBlock Text="deg." Foreground="White" FontSize="20" VerticalAlignment="Center" Margin="10,0,0,0" Opacity="0.25"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="15,10,0,5">
                                    <TextBlock Text="{x:Static res:Resources.String75}" Foreground="White" FontSize="20" VerticalAlignment="Center"/>
                                    <!-- ボール直径は変更できないよう無効(2025.10.31yori) -->
                                    <TextBox Width="70"
                                        FontSize="20"
                                        Margin="20,0,0,0"
                                        VerticalContentAlignment="Center"
                                        Background="Black"
                                        Foreground="White"
                                        Text="{Binding BallDiameter, UpdateSourceTrigger=PropertyChanged}"
                                        IsEnabled="False"/>
                                    <TextBlock Text="mm" Foreground="White" FontSize="20" VerticalAlignment="Center" Margin="10,0,0,0"/>
                                </StackPanel>
                                <StackPanel Orientation="Horizontal" Margin="15,10,0,5">
                                    <TextBlock Text="{x:Static res:Resources.String76}" Foreground="White" FontSize="20" VerticalAlignment="Center"/>
                                    <ComboBox ItemsSource="{Binding BallItems}" SelectedIndex="{Binding BallIndex}" Width="152" FontSize="20" Margin="10,0,0,0" SelectionChanged="ComboBox_SelectionChanged"/>
                                </StackPanel>
                                <!-- ゲージタイプ追加(2025.10.27yori) -->
                                <!-- キャリブレーション未作成のため、ゲージタイプ無効(2025.10.31yori) -->
                                <StackPanel Orientation="Horizontal" Margin="15,10,0,5">
                                    <TextBlock Text="{x:Static res:Resources.String37}" Foreground="White" FontSize="20" VerticalAlignment="Center" Opacity="0.25"/>
                                    <TextBlock Text="：" Foreground="White" FontSize="20" VerticalAlignment="Center" Opacity="0.25"/>
                                    <ComboBox ItemsSource="{Binding GaugeItems}" SelectedIndex="{Binding GaugeIndex}" Width="152" FontSize="20" Margin="10,0,0,0" IsEnabled="False" Opacity="0.25"/>
                                </StackPanel>
                            </StackPanel>

                            <!-- 仮の画像スペース -->
                            <!-- Margin="46,10,46,47"→Margin="45,0,15,40"へ変更(2025.10.27yori) -->
                            <Border Background="#DDD" Margin="45,0,15,40" CornerRadius="6" Grid.Column="1">
                                <Image Source="{Binding ImageSource}"
                                   Stretch="Uniform"
                                   VerticalAlignment="Center"
                                   HorizontalAlignment="Center"/>
                            </Border>

                            <StackPanel Grid.Column="2" VerticalAlignment="Top" Orientation="Vertical" Margin="0,0,0,0">
                                <!-- Margin="0,50,0,5"→Margin="0,0,0,5"へ変更(2025.10.27yori) -->
                                <!-- キャプション開始未作成のため、無効(2025.10.31yori) -->
                                <!-- プローブ登録追加(2025.10.31yori) -->
                                <Button Content="{x:Static res:Resources.String59}"
                                    Margin="0,0,0,5"
                                    Width="215" Height="40" Click="Click_StartBtn" Style="{StaticResource PanelStartButtonStyle}" FontSize="20" IsEnabled="False" Opacity="0.25"/>
                                <Button Content="{x:Static res:Resources.String70}"
                                    Margin="0,10,0,5"
                                    Width="215" Height="40" Click="Click_ResistBtn" Style="{StaticResource PanelDoneButtonStyle}" FontSize="20"/>
                                <Button Content="{x:Static res:Resources.String56}"
                                    Margin="0,10,0,5"
                                    Width="215" Height="40" Click="Click_CancelBtn" Style="{StaticResource PanelCancelButtonStyle}" FontSize="20"/>
                            </StackPanel>
                        </Grid>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Border>
    </Grid>
</local:PanelBase>
