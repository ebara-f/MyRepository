<Window x:Class="VecApp.DlgHistory"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Closing="Terminate"
        Loaded="Window_Loaded"
        BorderThickness="1" BorderBrush="LightGray"
        Title="History" Height="400" Width="300"
        ResizeMode="NoResize">

    <Grid Background="Black">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="40" />
        </Grid.RowDefinitions>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>


            <TextBox Grid.Row="0" Grid.Column="0" Text="{Binding TextHistory, UpdateSourceTrigger=PropertyChanged}" IsReadOnly="True" Margin="0,0,0,0" VerticalAlignment="Center" Height="300" FontSize="15" TextAlignment="Left" Background="#2A2A2A" Foreground="White"/>


        </Grid>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center">
            <Button Margin="4,0" Width="70" Height="30" Content="OK"         Click="ButtonClickOk" Background="DarkGray" Foreground="White"/>
        </StackPanel>
    </Grid>
 
</Window>
